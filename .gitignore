node_modules

# WhatsApp session material — never commit
whatsapp-auth/
dist
*.local
.env
.env.*
!.env.example
.DS_Store

# Local Netlify folder
.netlify

.vercel
.env*.local

# Claude local worktrees (nested repos; not part of Sigflo source)
.claude/worktrees/

# Continue IDE local config (models, MCP servers, secrets placeholders)
.continue/

# OpenCode config (may contain MCP server secrets / tokens)
opencode.json
.opencode/
