node_modules
dist
*.local
.env
.env.*
.DS_Store

# Local Netlify folder
.netlify

.vercel
.env*.local

# Claude local worktrees (nested repos; not part of Sigflo source)
.claude/worktrees/

# Continue IDE local config (models, MCP servers, secrets placeholders)
.continue/

# OpenCode config (may contain tokens in local dev)
opencode.json
.opencode/
