import type { SigfloActivePosition } from '@/types/position';

/** Demo repository seed — empty: no synthetic active positions when not exchange-backed. */
export const MOCK_SIGFLO_POSITIONS: SigfloActivePosition[] = [];
